name: CodeQL Configuration

# Note: path filtering is done via filter-sarif action in the workflow
# as paths-ignore doesn't work reliably for C# generated files

query-filters:
  - exclude:
      id: cs/catch-of-all-exceptions